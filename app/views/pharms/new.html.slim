- provide(:title, t('page.contact.new'))
.container
  .page-header
    h1
      = yield(:title)
  .row
    .col-sm-6.col-sm-offset-3
      = render 'pharms/shared/form'
  hr
  = link_to contacts_path, class: 'btn', title: t('back') do
    span.glyphicon.glyphicon-triangle-left
  br
  hr